function peco-cd() {
  item=$( (
    cat ~/.zshrc.d/peco-cd.txt
    ghq list --full-path
  ) | peco | head -n 1)

  if [ ! "${item}x" = "x" ]; then
    cd "${item}"
    BUFFER=""
    zle accept-line
  fi
}

zle -N peco-cd
bindkey "^[s" peco-cd
